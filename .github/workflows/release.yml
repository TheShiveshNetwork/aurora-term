name: 'release'
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - name: Create or reuse release
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          TAG="${{ github.ref_name }}"
          TITLE="Aurora $TAG"
          NOTES="See the assets to download this version and install."
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release edit "$TAG" --title "$TITLE" --notes "$NOTES" --draft=false
          else
            gh release create "$TAG" --title "$TITLE" --notes "$NOTES"
          fi

  build-tauri:
    needs: [create-release]
    env:
      APPIMAGE_EXTRACT_AND_RUN: '1'
      DEPLOY_GTK_VERSION: '3' # ldd fix: skip GTK auto-detection
      NO_STRIP: '1'
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest'
            args: '--target aarch64-apple-darwin'
          - platform: 'macos-latest'
            args: '--target x86_64-apple-darwin'
          - platform: 'ubuntu-22.04'
            args: '--verbose'
          - platform: 'ubuntu-22.04-arm'
            args: '--verbose --bundles deb' # ldd fix: skip AppImage/linuxdeploy
          - platform: 'windows-latest'
            args: ''
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v7

      - name: install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04' || matrix.platform == 'ubuntu-22.04-arm'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            build-essential \
            curl \
            wget \
            file \
            libxdo-dev \
            libssl-dev \
            libsecret-1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            xdg-utils \
            zsync \
            libgtk-3-dev \
            pkg-config \
            libglib2.0-dev \
            libfuse2 \
            qemu-user-static

      # ldd fix: let the ARM host resolve the aarch64 sidecar
      - name: install aarch64 cross toolchain (ubuntu arm only)
        if: matrix.platform == 'ubuntu-22.04-arm'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gcc-aarch64-linux-gnu \
            g++-aarch64-linux-gnu \
            libc6-dev-arm64-cross


      - uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: setup node
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: 'pnpm'

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './tauri -> target'

      - name: install bun
        uses: oven-sh/setup-bun@v2

      - name: install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Extract version from tag
        id: version
        shell: bash
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
          else
            VERSION=$(node -e "console.log(JSON.parse(require('fs').readFileSync('tauri/tauri.conf.json','utf8')).version)")
            echo "VERSION=$VERSION" >> "$GITHUB_OUTPUT"
          fi

      - name: Patch tauri.conf.json version
        shell: bash
        run: |
          node -e "
            const fs = require('fs');
            const c = JSON.parse(fs.readFileSync('tauri/tauri.conf.json','utf8'));
            c.version = '${{ steps.version.outputs.VERSION }}';
            fs.writeFileSync('tauri/tauri.conf.json', JSON.stringify(c,null,2) + '\n');
          "

      - uses: tauri-apps/tauri-action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: './tauri'
          tagName: ${{ github.ref_name }}
          releaseName: 'Aurora ${{ github.ref_name }}'
          releaseBody: 'See the assets to download this version and install.'
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}
